# 留资/埋点提交的目标 API(EnsureOK 现有服务)。
# 生产构建把它编进产物;跨域提交依赖 EnsureOK 侧对 /api/startup-leads 与 /api/events 开放的 CORS。
# 本地 dev 不必设(vite proxy 已把 /api 转到线上,免 CORS)。
VITE_API_BASE=https://ensureok.ai

# ===== OpenAI(RAG 嵌入 + 方案生成)=====
# 只在后端/脚本读取,绝不进前端产物(无 VITE_ 前缀)。把真实 key 填到 .env(本文件是模板;.env 已 gitignore,永不提交)。
OPENAI_API_KEY=

# 国内直连不了 api.openai.com 时,填你的 OpenAI 兼容中转/网关基址(以 /v1 结尾)。
# 例:https://your-proxy.example.com/v1  (Azure OpenAI 的部署式路径需另配适配器,告知我即可)
# OPENAI_BASE_URL=https://api.openai.com/v1

# 嵌入模型与维度(默认 text-embedding-3-small / 1536)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_EMBEDDING_DIM=1536

# 嵌入后端:openai | stub。缺省:有 OPENAI_API_KEY 则 openai,否则 stub(离线桩,先跑通流程用)。
# EMBEDDING_PROVIDER=openai

# RAG 语料源目录(默认项目同级 ../保险资料);产品数据库源目录见 CATALOG_SOURCE_ROOT。
# RAG_SOURCE_ROOT=../保险资料
